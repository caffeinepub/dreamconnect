// Re-export useInternetIdentity from the caffeineai platform package.
// This file exists so that local imports like `import { useInternetIdentity } from "./hooks/useInternetIdentity"`
// continue to work after migration.
export { useInternetIdentity } from "@caffeineai/core-infrastructure";
